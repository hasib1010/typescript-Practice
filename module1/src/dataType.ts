// string
const firstName: string = "hasib"

// number
const roll: number = 10;

// bolean
const isAdmin: boolean = true;

// undefined
const x: undefined = undefined;

// unll
const y: null = null


let friends: string[] = ["shakil", "emon", "arif"]
friends.push('a')

const profile: [string, number, boolean] = ["Hasib", 20, false]